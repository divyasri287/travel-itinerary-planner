/**
 * Phase 1 placeholder — the Trip model and CRUD API are built in Phase 3.
 * This route exists now so navigation and protected routing can be tested.
 */
const MyTrips = () => {
  return (
    <div>
      <div className="page-header">
        <h1 className="page-title">My Trips</h1>
        <p className="page-subtitle">Your trips will be listed here starting in Phase 3.</p>
      </div>
      <div className="card">
        <p style={{ margin: 0, color: "var(--color-text-muted)", fontSize: 14 }}>
          No trip data yet — this page is a placeholder confirming routing works.
        </p>
      </div>
    </div>
  );
};

export default MyTrips;
